version: 2.1

orbs:
    node: circleci/node@1.1.6
    js: cambiahealth/janus-javascript@1
    sb: cambiahealth/stack-build@1
jobs:
    lint-and-build:
        executor:
            name: node/default
        environment:
            JOBS: 1
        steps:
            - checkout
            - js/configure-npm:
                  target: .npmrc
            - restore_cache:
                  name: Restore NPM Package Cache
                  keys:
                      - npm-packages-{{ checksum "package-lock.json" }}
            - run:
                  name: Install NPM Packages
                  command: npm install
            - save_cache:
                  name: Save NPM Package Cache
                  key: npm-packages-{{ checksum "package-lock.json" }}
                  paths:
                      - ~/.cache/npm
            - run:
                  name: Lint Typescript
                  command: npm run lint
            - run:
                  name: Build React
                  command: npm run build:app
            - run:
                  name: Build Styleguide
                  command: npm run build:storybook
            - persist_to_workspace:
                  root: ./
                  paths:
                      - build/
                      - styleguide/
    acceptance-tests:
        executor:
            name: node/default
        environment:
            JOBS: 1
        steps:
            - attach_workspace:
                  at: ./
            - checkout
            - js/configure-npm:
                  target: .npmrc
            - restore_cache:
                  name: Restore NPM Package Cache
                  keys:
                      - npm-packages-{{ checksum "package-lock.json" }}
            - run:
                  name: Install NPM Packages
                  command: npm install
            - save_cache:
                  name: Save NPM Package Cache
                  key: npm-packages-{{ checksum "package-lock.json" }}
                  paths:
                      - ~/.cache/npm
            - run:
                  name: Run Cucumber tests
                  command: npm run test:acceptance

workflows:
    version: 2

    # Build and Test (all non-development/master branches)
    build-and-test:
        jobs:
            - lint-and-build:
                  context: cambia
                  filters:
                      branches:
                          ignore:
                              - master
                              - development
            - acceptance-tests:
                  context: cambia
                  requires:
                      - lint-and-build

    # Deploy to Janus (only development/master)
    janus-pipeline:
        jobs:
            - sb/preflight:
                  filters:
                      branches:
                          only:
                              - master
                              - development
            - sb/normalize:
                  context: cambia
                  requires:
                      - sb/preflight
            - lint-and-build:
                  context: cambia
                  requires:
                      - sb/preflight
            - acceptance-tests:
                  context: cambia
                  requires:
                      - sb/preflight
                      - lint-and-build
            - sb/stack-formation:
                  context: cambia
                  get-s3-contents-from-workspace: true
                  requires:
                      - sb/preflight
                      - sb/normalize
                      - lint-and-build
            - sb/stack-review:
                  context: cambia
                  requires:
                      - sb/preflight
                      - sb/normalize
            - sb/publish-artifacts:
                  context: journi-web-app
                  requires:
                      - sb/preflight
                      - sb/stack-formation
                      - sb/stack-review
