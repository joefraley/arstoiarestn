const { Given, When, Then } = require("cucumber")
// const { navigateToPath } = require("./../support/basic")

Given("I am on the home page", async () => {
    // navigateToPath("/")
})
When("I click on Settings in the sidebar", async () => {})
Then("I will be on the settings page", async () => {})
